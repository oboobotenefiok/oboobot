/target
**/*.rs.bk
*.pdb

deep.rs

# The demo binary writes its cursor file under the OS temp directory, not
# here, but excluding a local state/ directory too in case a future real
# deployment config points it at one relative to the project root.
/state
